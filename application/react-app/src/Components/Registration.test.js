import {render, screen} from '@testing-library/react';
import Registration from '../Components/Registration';

test("Registration functionality", () => {
    
    test("Should capture content correctly onClick", () => {
        render(<Registration/>);
        const submit = screen(onClick());
        expect(setState).toBe("Testing");
    });

    test("Should capture content correctly onClick", () => {
        render(<Registration/>);
        const input_descrition = "test";
        const input_price = "20";
        const input_address = "test";
        const input_start = "test";
        const input_end = "test";
        const input_equipment = "Outdoor Eqipment"
        const imput_location = "test"
        expect(res.send('msg')).toBe("Successfully posted");
    });

});


