import {render, screen} from '@testing-library/react';
import PostPage from '../Components/PostPage';


test("Listing Functionality", () => {
    
    test("Should dubmit onClick", () => {
        render(<PostPage/>);
        const button = screen(OnClick());
        expect(setState).toBe("Testing");
    });

    test("Content should post successfully", () => {
    render(<PostPage/>);
    const input_
    const input_birth = "10/14/1996";
    const input_address = "2047 100th Ave";
    const input_password = "Stong)Password47";
    const input_ConfirmPassword = "Stong)Password47";
    const zipCode = "94111"
    expect(res.send('msg')).toBe("Successfully Register");
    });

});
